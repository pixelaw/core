#[cfg(test)]
pub mod helpers;

#[cfg(test)]
mod tests {
    mod app_paint;
    mod app_snake;
    mod app_player;
    mod app_house;
    mod area;
    mod base;
    mod interop;
    mod pixel_area;
    mod queue;
}
