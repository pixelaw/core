---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.projectName }}-{{ .Values.nameSpace }}-{{ .Values.appType.frontend }} 
  namespace: {{ .Values.projectName }}-{{ .Values.nameSpace }}
  labels:
    app: {{ .Values.projectName }}-{{ .Values.nameSpace }}-{{ .Values.appType.frontend }} 
spec:
  replicas: {{ .Values.replicaSet }}
  selector:
    matchLabels:
      app: {{ .Values.projectName }}-{{ .Values.nameSpace }}-{{ .Values.appType.frontend }} 
  template:
    metadata:
      labels:
        app: {{ .Values.projectName }}-{{ .Values.nameSpace }}-{{ .Values.appType.frontend }} 
    spec: 
      containers:
      - name: {{ .Values.projectName }}-{{ .Values.nameSpace }}-{{ .Values.appType.frontend }} 
        image: {{ .Values.dockerImage }}
        imagePullPolicy: Always
        ports:
        - containerPort: {{ .Values.webappContainerService.port }}       
        env:
        - name: PUBLIC_NODE_URL
          value: "https://api.cartridge.gg/x/pixelaw/katana"                  
        - name: PUBLIC_TORII
          value: "https://api.cartridge.gg/x/pixelaw/torii"           
        - name: SLOT_KATANA
          value: "https://api.cartridge.gg/x/pixelaw/katana"           
        - name: SLOT_TORII
          value: "https://api.cartridge.gg/x/pixelaw/torii"    
        - name: SEED
          value: "17885659508068513399"           
        - name: TOTAL_ACCOUNTS
          value: "4"       
        - name: WORLD_ADDRESS
          value: "0x6395ccab8983e6598b8d54bac18cadb63d04b8e4631bde418a2cfb504b59a89"
                 
        # resources:
        #   requests:
        #     memory: "512Mi"
        #     cpu: "500m" 
        #   limits:
        #     memory: "1Gi"
        #     cpu: "1000m" 
        volumeMounts:
        - name: keiko-manifests
          mountPath: /keiko/manifests

      volumes:

      - name: keiko-manifests
        persistentVolumeClaim:
          claimName: pixelaw-core-main--pvc          